import React from "react";

// If onNext and onPrev function are send as a prop then we need to create buttons here in the component.

const component1 = () => {
  return <h1>Component 1</h1>;
};

export default component1;
